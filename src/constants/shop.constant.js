const shopWithCredentialAttributes = [
  '_id',
  'name',
  'email',
  'password',
  'status',
  'roles',
];

const shopAttributes = ['_id', 'name', 'email', 'status'];

module.exports = {
  shopWithCredentialAttributes,
  shopAttributes,
};
